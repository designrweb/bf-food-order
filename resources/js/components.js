import initConsumerImageUploadForm from './components/consumer-image-upload-form/consumerImageUploadForm'

function init() {
    // initConsumerImageUploadForm();
}

document.addEventListener('DOMContentLoaded', function () {
    init();
});
